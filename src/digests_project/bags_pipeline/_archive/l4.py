
from __future__ import annotations
from typing import List, Dict, Tuple

# def build_L4_topic(idx_rows: List[Dict], topic_id: str, win_key: str, *, min_salience: float = 0.25) -> Tuple[Dict, str]:
#     """Return (manifest_dict, mdx_text) for a topic digest in a given window."""
#     manifest = {"digest": {"level":"L4","title": f"Topic: {topic_id} — {win_key}"}, "validated": False}
#     mdx = f"# Topic — {topic_id}\n\n_TODO: render content for {win_key}_\n"
#     return manifest, mdx
