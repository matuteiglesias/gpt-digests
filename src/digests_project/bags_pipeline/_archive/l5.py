
from __future__ import annotations
from typing import List, Dict, Tuple

# def build_L5_monthly(idx_rows: List[Dict], month_key: str, *, total_cap: int = 40) -> Tuple[Dict, str]:
#     """Return (manifest_dict, mdx_text) for the monthly portfolio digest."""
#     manifest = {"digest": {"level":"L5","title": f"Monthly — {month_key}"}, "validated": False}
#     mdx = f"# Monthly — {month_key}\n\n_TODO: render highlights & metrics_\n"
#     return manifest, mdx
